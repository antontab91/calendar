import React from 'react';

import AddBtn from './AddBtn';
import Navigation from './Navigation';
import CurrentDate from './CurrentDate';



const Header = () => {
    return (
        <div className="header">
            <AddBtn prop1={} prop2={} />
            <Navigation prop1={} prop2={} />
            <CurrentDate prop1={} prop2={} />
        </div>
    )
}